# Mara's dotfiles

initial setup (run 1x)

```bash
brew install gh # install github cli
gh repo fork alipatti/dotfiles # fork repo
gh repo clone mcasebeer/dotfiles # clone your fork
cd dotfiles
git merge for-mara # merge mara branch into main
```

script to install everything into the correct place

```bash
./setup
```


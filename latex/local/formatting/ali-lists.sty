% This package:
% - sets enumerate labels to roman, e.g. i), ii)
% - sets itemize labels to dashes, e.g. -

\RequirePackage{enumitem}

\setlist[enumerate]{
	label=\roman*),
}

\setlist[itemize]{
	label=-,
}
